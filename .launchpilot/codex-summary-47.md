Updated [README.md](/workspaces/task-47/repo/README.md) only.

It now describes the repo as a static GitHub Pages dashboard, documents the `launchpilot` / `launchpilot-data` / `launchpilot-dashboard` architecture, lists the production dashboard sections, adds quickstart and troubleshooting steps, and notes that fallback sample data should remain if `tasks.json` fetch fails.

No secrets, env files, tokens, or unrelated files were edited. No tests were run since this was a documentation-only change.